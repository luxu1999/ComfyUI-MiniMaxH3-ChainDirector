[project]
name = "ComfyUI-MiniMaxH3-ChainDirector"
version = "0.1.0"
description = "MiniMax H3 Chain Director (CN/EN): auto split long video into r2v+i2v segments with first-frame locking, stitch video+audio in one node. 链式导演台：自动分段拼接 MiniMax H3 长视频"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "luxu1999" }]
keywords = ["comfyui", "minimax", "minimax-h3", "video", "custom-node"]

[project.urls]
Repository = "https://github.com/luxu1999/ComfyUI-MiniMaxH3-ChainDirector"

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.comfy]
PublisherId = "luxu1999"
DisplayName = "MiniMax H3 Chain Director｜链式导演台"
